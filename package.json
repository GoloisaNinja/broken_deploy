{
  "name": "broken_deploy_example",
  "version": "1.0.0",
  "description": "an example of broken deploy with railway",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install --prefix client && npm run build --prefix client"
  },
  "keywords": [],
  "author": "Jon Collins",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  }
}
